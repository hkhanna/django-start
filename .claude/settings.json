{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath'); if [[ \"$f\" == *.py ]]; then uv run ruff check --fix \"$f\"; uv run ruff format \"$f\"; fi 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
